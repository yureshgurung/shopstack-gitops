apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopstack.fullname" . }}-test-connection"
  labels:
    {{- include "shopstack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopstack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
