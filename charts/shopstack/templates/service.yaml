apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopstack.fullname" . }}
  labels:
    {{- include "shopstack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopstack.selectorLabels" . | nindent 4 }}
