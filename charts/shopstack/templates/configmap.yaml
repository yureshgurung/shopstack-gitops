apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "shopstack.fullname" . }}-config
  labels:
    {{- include "shopstack.labels" . | nindent 4 }}
data:
  LOG_LEVEL: {{ .Values.config.backend.logLevel | quote }}
  API_BASE_URL: {{ .Values.config.frontend.apiBaseUrl | quote }}